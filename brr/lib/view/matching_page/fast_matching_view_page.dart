import 'package:flutter/material.dart';

class FastMatchingPageView extends StatelessWidget {
  const FastMatchingPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Fast Matching View Page'),
    );
  }
}
